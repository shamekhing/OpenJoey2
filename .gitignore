# Build directories
build/
Build/
build-*/

# The six split repos are independent git repositories living side-by-side
# here; this umbrella repo does not track their contents.
openjoey-*/

# Editor / temp noise
.cache/
*.swp
*.tmp
*.log
